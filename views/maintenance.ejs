<!doctype html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title || "Mantenimiento" %></title>
    <style>
        body{font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial; padding:40px; max-width:720px; margin:auto;}
        .box{border:1px solid #ddd; border-radius:14px; padding:24px;}
        h1{margin:0 0 10px;}
        p{margin:0;}
    </style>
</head>
<body>
<div class="box">
    <h1><%= title || "Mantenimiento" %></h1>
    <p><%= message || "Estamos preparando el sitio." %></p>
</div>
</body>
</html>
